<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/static/global.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body class="container mx-auto p-4">
    <h2>Danh sách sản phẩm</h2>
    
    <div class="flex justify-center container">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4 mb-6">
        {{range .products}}
            <div class="border p-4 rounded-lg shadow">
                <img src="{{.ImageUrl}}" alt="{{.Name}}" class="w-full aspect-square rounded object-cover mb-2">
                <h3 class="text-lg font-bold mb-2">{{.Name}}</h3>
                <p class="text-green-600 font-semibold text-xl">{{formatPriceWithCommas .Price}}</p>
                <p class="text-gray-600">Số lượng: {{.Quantity}}</p>
                <div class="mt-4 flex gap-2">
                    <a href="/products/{{.Id}}" class="bg-blue-500 text-white px-3 py-1 rounded text-sm">Edit</a>
                    <button onclick="deleteProduct({{.Id}})" class="bg-red-500 text-white px-3 py-1 rounded text-sm">Delete</button>
                </div>
            </div>
        {{else}}
            <div class="col-span-full text-center py-8">
                <p class="text-gray-500">Không có sản phẩm nào trong danh mục này.</p>
            </div>
        {{end}}
    </div>
    </div>
    
    <div class="mb-4">
        <p>Tổng: {{.totalCount}} sản phẩm | Trang {{.currentPage}}/{{.totalPages}}</p>
    </div>

    {{if gt .totalPages 1}}
    <div class="flex justify-center items-center gap-2 mt-6">
        {{if .hasPrev}}
            <a href="/category/{{.categoryID}}?page={{sub .currentPage 1}}" 
               class="px-3 py-2 bg-gray-200 text-gray-700 rounded hover:bg-gray-300">
                « Trước
            </a>
        {{end}}
        
        {{range $i := (pageRange .currentPage .totalPages)}}
            {{if eq $i $.currentPage}}
                <span class="px-3 py-2 bg-blue-500 text-white rounded">{{$i}}</span>
            {{else}}
                <a href="/category/{{$.categoryID}}?page={{$i}}" 
                   class="px-3 py-2 bg-gray-200 text-gray-700 rounded hover:bg-gray-300">
                    {{$i}}
                </a>
            {{end}}
        {{end}}
        
        {{if .hasNext}}
            <a href="/category/{{.categoryID}}?page={{add .currentPage 1}}" 
               class="px-3 py-2 bg-gray-200 text-gray-700 rounded hover:bg-gray-300">
                Sau »
            </a>
        {{end}}
    </div>
    {{end}}
    
    <script>
        async function deleteProduct(id) {
            if(confirm('Bạn có chắc chắn muốn xoá sản phẩm này không?')) {
                try {
                    const res = await fetch('/products/' + id, {
                        method: 'DELETE'
                    });
                    if(res.ok) {
                        alert('Xoá sản phẩm thành công!');
                        window.location.reload();
                    } else {
                        alert('Xoá sản phẩm thất bại!');
                    }
                } catch (error) {
                    alert('Có lỗi xảy ra!');
                }
            }
        }
    </script>
</body>
</html>